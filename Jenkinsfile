pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Apka build command yahan likhein
                sh 'make build'
            }
        }
    }
}
